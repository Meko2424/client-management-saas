spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mini_crm
    username: postgres
    password: solo2424

  jpa:
    hibernate:
      ddl-auto: validate   # IMPORTANT: Flyway controls schema
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080